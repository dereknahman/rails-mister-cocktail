<h1><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |c| %>
<div class="dose-description">
  <%= c.description %> <%= "of " %><%= c.ingredient.name %>
  </div>
<% end %>

<%= render "doses/form", cocktail: @cocktail, dose: @dose %>


<%= link_to "Go to index", cocktails_path, class: "btn btn-primary" %>
